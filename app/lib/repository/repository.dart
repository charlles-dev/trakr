/// Gerenciamento do cache local (Isar/SQLite).
///
/// Planejado:
/// - `local_cache.dart` - persistência local do inventário recebido via BLE.
///   O firmware continua sendo a fonte da verdade.
library;
