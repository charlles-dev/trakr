name: trakr_app
description: "Trakr - O Visualizador da Maleta de Ferramentas Inteligente"
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.0
  flutter_local_notifications: ^17.0.0
  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0
  provider: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  isar_generator: ^3.1.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
  assets:
    - assets/